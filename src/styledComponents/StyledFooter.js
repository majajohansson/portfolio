import styled from 'styled-components';

const StyledFooter = styled.footer`
  text-align: center;
  height: 120px;
  line-height: 120px;
  font-size: 14px;
  position: absolute;
  bottom: 0;
  width: 100%;
`;

export default StyledFooter;
